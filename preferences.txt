# "anti" Subsystem Architectural Preferences & Coding Standards

## Mandatory First Step
- BE AWARE OF ARCHITECTURAL PREFERENCES:
  - The agent must be fully aware of the architectural preferences defined in `preferences.txt`. The rules herein are persistent, but there is no need to execute a file read tool call on it at the start of every single chat turn once established.

## 1. Zero-Allocation & Off-Heap Memory Philosophy
- NO 'new' KEYWORD FOR DATA / PRIMITIVES:
  - Avoid Java heap allocations (`new Object()`, `new int[]`, `new String()`, etc.) in hot paths and system code.
  - Data structures, primitives, strings, matrices, vectors, arrays, and native resources MUST be allocated off-heap using raw pointers (`long` memory addresses) managed by Java FFM (`java.lang.foreign`), `ForeignMemory`, `Arena`, and `MemoryRegistry`.

- NO OBJECT WRAPPING OR BOXING:
  - Never wrap primitive types into standard Java object wrappers (`Integer`, `Double`, `Float`, etc.).
  - Pass raw `long` memory pointers and primitive types (`int`, `long`, `float`, `double`).
  - Access fields directly via raw memory address offsets (`GLOBAL_MEMORY.get(...)` / `GLOBAL_MEMORY.set(...)` or VarHandles).

## 2. Pure Foreign Function & Memory (FFM) Standards
- Pure `java.lang.foreign` API usage:
  - Leverage `MemorySegment`, `Arena`, `Linker`, `ValueLayout`, `FunctionDescriptor`, `VarHandle`, and `MethodHandle`.
  - Global flat 64-bit virtual memory space addressing (`MemorySegment.ofAddress(0).reinterpret(Long.MAX_VALUE)`).
  - Use lock-free / lockless free-lists with ABA-tagged generation pointers (top 16 bits = generation tag, bottom 48 bits = raw pointer).
  - C-style manual memory lifecycle (`malloc`/`free` downcalls) combined with pooled Arenas for fixed-slot sizes (e.g. small/medium/large slots in `primitive.string`, singletons/arrays/matrices in `primitive.Int`).

## 3. Preservation of Developer Comments & Intent
- DO NOT REMOVE OR REWRITE DEVELOPER COMMENTS:
  - Preserve all original author comments, notes, annotations, humor, and design notes intact (e.g., "// god, the lens of all things...", "// literally c flavored hell java version").
  - Do NOT replace handwritten developer notes with generic, bland auto-generated docstrings.
  - When extending or refactoring code, respect surrounding code formatting, naming conventions, and comment styles.
- CLEAN TOP-LEVEL IMPORTS (NO INLINE FULLY-QUALIFIED CLASS NAMES):
  - Do NOT write inline fully-qualified package names in code bodies (e.g. avoid `package.Class.method()` or `package.Class.field`).
  - Always put clean imports at the top of the file and use simple `Class.method()` in the code body (allowing IDEs like IntelliJ to manage imports automatically).

## 4. Subsystem & Threading Guidelines
- Thread Safety: Ensure off-heap memory updates are atomic using `VarHandle` operations (`compareAndSet`, `getAndSet`), lockless pools, or dedicated execution threads (e.g., `DrawThread`, `NetworkingThread`, `ScriptingThread`).
- Zero-GC Impact: Keep runtime data structures off-heap to prevent JVM Garbage Collection pauses.

## 5. Private / Regurgitation Files & Privacy
- REGURGITATION & PRIVATE FILES ARE STRICTLY IGNORED:
  - Regurgitation code dumps, reference files, and aggregate files (e.g., `src/regurgitation/`, `code.txt`, `*.regurgitation`) are strictly private.
  - They MUST be listed in `.gitignore` and NEVER committed to Git repositories.

## 6. Git Commit & Push Workflow
- AUTOMATIC LOCAL COMMITS AFTER TEST VERIFICATION:
  - Automatically create a local Git commit after completing and verifying code modifications with `ScratchTest.java`.
  - Commits MUST be made per-file (create a separate local commit for each modified file rather than grouping multiple changed files into a single commit).
  - DO NOT execute `git push` automatically. Accumulate commits locally and only push to remote when explicitly requested by the user ("push", "push it all", etc.).
  - Always preserve private author metadata (`vexgraph-dev <305705943+vexgraph-dev@users.noreply.github.com>`).
- DUAL REPOSITORY ARCHITECTURE:
  - The project operates with two active Git repositories:
    1. Parent Repository: `anti` (root directory `/Users/vexgraph/IdeaProjects/anti/`).
    2. Nested Repository: `anti-api-haven` (located inside the ignored folder `/Users/vexgraph/IdeaProjects/anti/src/api/`).
  - Code changes made inside `src/api/` must be staged, committed, and managed inside the nested `anti-api-haven` Git repository.
  - All other project files must be managed in the parent `anti` Git repository.
  - Both repositories must use the same `vexgraph-dev` author metadata for commits.

## 7. Mandatory Code Verification & Scratch Testing
- MANDATORY RUNTIME TESTING IN ScratchTest.java:
  - Every time new code or subsystems are created or modified, empirical compilation and execution testing MUST be performed via `ScratchTest.java` (or dedicated test suites).
  - Never declare a task or feature completed without compiling, executing, and verifying clean runtime output.

## 8. Custom Annotation Usage Rules
- `@Draft` (`annotation.Draft`):
  - Must be applied to any class, interface, or method that is newly created, experimental, or in a draft state (not yet ready for production release).
- `@Intention(...)` (`annotation.Intention`):
  - Must be applied to non-obvious architecture, core headers, or complex algorithms to state the design rationale and intent explicitly.
- `@Required` (`annotation.Required`):
  - Must be applied to mandatory class ID definitions (e.g. `public static final int CLASS_ID`), required framework hooks, or mandatory class contracts.
- `@Volatile` (`annotation.Volatile`):
  - Must be applied to framework-level classes that mutate engine state, modify core layout/memory contracts, or control thread dispatching (e.g. MemoryRegistry, off-heap collections, thread locks).
- `@HotCode` (`annotation.HotCode`):
  - Must be applied to classes or functions that require modification or updates every time a new class is added or registered in the framework (e.g. TypeRegister, Stride, ForeignMemory).

## 9. Off-Heap Bit-Packed Type System Standards
- 32-Bit Type ID Bit-Masking (`oop.TypeRegister`):
  - Upper 8 Bits (`0xFF000000`): Form/Kind Mask
    - `0xAA000000` = `FORM_SINGLETON` (Scalar primitive/struct)
    - `0xBB000000` = `FORM_ARRAY` (Flat primitive/data array)
    - `0xCC000000` = `FORM_POINTER` (Array of 64-bit off-heap pointers / matrix)
  - Lower 24 Bits (`0x00FFFFFF`): Unique Class ID (e.g. `ID_INT32 = 0x000001`)
- Off-Heap Header Protocol:
  - Every off-heap payload address `userPtr` must be self-describing, prefixed with its 32-bit type ID and length header before the user memory boundary.

## 10. Next Development Steps (as of 2026-08-03)

### Subsystem Completion Status

#### ✅ PRODUCTION-READY (fully implemented + ScratchTest verified)
- `primitive.*` — All scalar types: Int, Long, Float, Double, Byte, Short, Bool, string, Brain (bfloat16), Fixed32 (q16.16), Fixed64 (q32.32), IntFloat, IntDouble, LongFloat, LongDouble, Pack. Singleton / Array / Matrix / CAS all working.
- `oop.TypeRegister`, `oop.Stride`, `oop.Struct` — 32-bit bit-masked type system, custom struct allocation (Singleton/Array/Matrix), volatile + CAS field access.
- `nio.ForeignMemory` — Global flat 64-bit memory space, freeAllClasses lifecycle.
- `struct.*` — List, Map, Set, Stack, Deque, Array, SparseSet, MinHeap, Collection (all off-heap, fully tested including struct-aware containers).
- `spatial.GridArray`, `spatial.CircularArray` — Flat and polar spatial partitioning, insert/query/update/remove.
- `thread.SpinLock`, `thread.RingBuffer` — Lockless primitives, fully tested.
- `thread.NetworkingThread`, `thread.DrawThread`, `thread.ScriptingThread`, `thread.ConsoleThread`, `thread.ThreadRegistry` — Worker pool lifecycle (invoke/run/stop/free/runAll/stopAll/freeAll).
- `variable.Variable`, `variable.SearchVariable` — Off-heap variable registry, O(n) search, Trie autocomplete.
- `relational.RelationalEngine` — Variable value get/set (Int/Float/Double/Long/String), compareAndSetString (CAS, thread-safe).
- `time.NanoTime`, `time.Clock`, `time.DateTime`, `time.Calendar` — Off-heap time tracking, virtual time scaling/pause, Hinnant algorithm date math.
- `util.Random`, `util.IndexRandom`, `util.Hash`, `util.Arrays` — PRNG (Xoshiro state), deterministic index-based random, FNV-1a/Murmur3 hashing, sort/binary-search.
- `lang.Class`, `lang.System`, `lang.StringBuilder`, `lang.FastMath`, `lang.Vec2/Vec3/Vec4`, `lang.Mat4`, `lang.Quaternion` — Math and type introspection helpers.
- `net.HTTPClient`, `net.HTTPServer`, `net.WebSocketClient`, `net.JSON`, `net.PollRequest`, `net.TransportProtocol` — Full off-heap HTTP/WS/JSON pipeline.
- `security.*` — SHA-256, HMAC-SHA256, SecureRandom, AES-GCM SecurePacket, ServerClientChecker (HMAC challenge + rate limiter), TouchID check.
- `cli.*` — CommandRegistry, CommandParser, Command, Console, Scanner (upcall stubs via FFM Linker).
- `input.Touch`, `input.Key`, `input.Mouse`, `input.KeyAdapter`, `input.MouseAdapter`, `input.TouchAdapter` — Off-heap multi-touch, keyboard, mouse event subsystems.
- `system.SystemDiscovery`, `system.DisplayInfo`, `system.DisplayMonitor` — Off-heap OS/hardware discovery, monitor enumeration.
- `entity.EntityManager` — Zero-GC entity ID recycling.
- `physics.PhysicsBody`, `physics.PhysicsEngine` — Off-heap physics body management.
- `geometry.*` — AABB, Frustum, Plane, Ray, Sphere, GeomMath.
- `window.macOSWindow`, `window.Window` — Platform window abstraction (macOS native, Linux/Windows stubs).
- `vulkan.*` — VK, Vulkan, Swapchain, RenderPass, CommandBuffer, CommandPool, Fence, Semaphore, VKBuffer, VKDeviceMemory, VKFramebuffer, VKImageView, VKPipeline, VKPipelineLayout, VKShaderModule.
- `annotation.*` — @Draft, @Intention, @Required, @Volatile, @HotCode.

#### 🔲 STUB / DRAFT (file exists, body not yet implemented)
- `engine.EngineLoop` — Functional interface stub only. The full engine tick loop (window open → thread launch → frame dispatch → shutdown sequence) is NOT yet built.
- `engine.Constraints` — Present but not tested in ScratchTest.
- `render.RenderMode` — Empty stub.
- `draw.*` — Canvas, Brush, Stroke, Bezier all empty stubs. No 2D draw pipeline exists yet.
- `animation.Joint` — Empty stub.
- `shader.ShaderMaker` — Empty stub. No GLSL/SPIR-V shader compilation pipeline yet.
- `entity.Entity`, `entity.Camera`, `entity.Light`, `entity.Player`, `entity.Member`, `entity.Group` — All empty stubs (only EntityManager is real).
- `spatial.BinaryTree`, `spatial.CubeArray`, `spatial.DAG`, `spatial.Graph`, `spatial.Octree`, `spatial.SphereArray` — Empty stubs.
- `math.*` — Separate math package (vs lang math helpers) — unclear scope, needs review.
- `mesh.*`, `material.*`, `light.*`, `shadow.*`, `post.*`, `terrain.*` — Assumed empty / early-draft (not inspected in depth).
- `ai.*` — Not inspected; assume stub or early design.
- `audio.*`, `daw.*` — AudioLayerTest and AudioTest exist in scratch but audio subsystem status not fully verified.

### Logical Next Steps (Priority Order)

1. **EngineLoop + Main Engine Bootstrap** (`engine.*`)
   - Implement a real `engine.Constraints` + `engine.EngineLoop` runtime.
   - Wire together: window open → DrawThread → ScriptingThread → NetworkingThread → ConsoleThread → tick loop → shutdown.
   - Entry point: ScratchTest should be able to boot and close the engine cleanly.

2. **Draw / Canvas Pipeline** (`draw.*`, `render.*`, `shader.*`)
   - Implement `draw.Canvas`, `draw.Brush`, `draw.Stroke` as off-heap structs.
   - Integrate with Vulkan (already has VK, Swapchain, RenderPass, Pipeline stubs).
   - Build a minimal `shader.ShaderMaker` for SPIR-V loading/compilation via LWJGL.

3. **Entity-Component System** (`entity.*`, `struct.SparseSet`)
   - Build full `entity.Entity`, `entity.Camera`, `entity.Light`, `entity.Player` off-heap structs backed by `oop.Struct`.
   - Implement component storage using `struct.SparseSet` (already exists) as off-heap ECS columns.
   - Tag entity slots in `entity.EntityManager` as @HotCode.

4. **Animation Skeleton** (`animation.Joint`)
   - Implement `animation.Joint` as an off-heap struct (position, rotation as Quaternion, parent index).
   - Build a skinning / joint-transform pipeline.

5. **Spatial Structures Completion** (`spatial.*`)
   - Implement `spatial.Octree` (3D equivalent of GridArray) — critical for 3D scene queries.
   - Implement `spatial.BinaryTree`, `spatial.DAG`, `spatial.Graph` for scene graph / dependency tracking.

6. **Audio Subsystem Verification** (`audio.*`, `daw.*`)
   - Run `scratch/AudioLayerTest.java` and `scratch/AudioTest.java` to verify compile + runtime.
   - Determine if `daw.*` (digital audio workstation) layer is in scope.

7. **GraalVM Native Image** ✅ — `AntiEngine-Native.app` (22MB, no JVM) builds and runs clean via `scratch/build_native.sh`. Window spawns, Vulkan + MoltenVK boots, input events captured, exits code 0. See §11 for compatibility rules.

## 11. GraalVM Native Image Compatibility Rules

These rules are MANDATORY for all code that must compile and run under GraalVM Native Image (`native-image`). Violating these will cause silent runtime failures or crashes that are extremely hard to debug.

### 11.1 FFM Downcall Registration
- EVERY `Linker.downcallHandle(...)` call used at runtime MUST have its exact `FunctionDescriptor` registered in `config.FFMRegistrationFeature` via `RuntimeForeignAccess.registerForDowncall(...)`.
- The `FunctionDescriptor` layout must be an EXACT structural match — same `ValueLayout` carriers in the same order. GraalVM matches by layout structure, not by name.
- This includes stdlib calls like `malloc` (`of(ADDRESS, JAVA_LONG)`) and `free` (`ofVoid(ADDRESS)`). Do not assume any downcall is auto-registered.
- When adding a new FFM downcall anywhere in the codebase, IMMEDIATELY add its `FunctionDescriptor` to `FFMRegistrationFeature.duringSetup(...)`.

### 11.2 Static Field Initialization (The `final` Trap)
- NEVER declare FFM handles (`MethodHandle`, `SymbolLookup`, `MemorySegment`, `VarHandle`) as `private static final` if the class is initialized at runtime.
- GraalVM constant-folds `static final` fields to their build-time value (`null`) in the compiled binary, even when `--initialize-at-run-time=...` is set. The runtime initializer runs, but all reads of the field are already hardcoded to `null` in machine code.
- Declare all FFM runtime handles as `private static` (non-final) only.
- Pure constants (`int`, `long`, `String` literals, enum values) are safe to keep `final`.

### 11.3 Class Initialization Timing
- All classes that call `Linker.nativeLinker()`, `SymbolLookup.libraryLookup(...)`, or any FFM downcall during their static initializer MUST be deferred to runtime.
- Add `--initialize-at-run-time=<fully.qualified.ClassName>` in `build_native.sh` for every such class.
- If a class `A` is initialized at build-time and it references class `B` (which has FFM handles), class `B` will also be pulled into build-time initialization. Always check the full dependency chain.

### 11.4 Native Framework Loading
- In a GraalVM native binary, macOS system frameworks (AppKit, QuartzCore, etc.) are NOT automatically loaded. The JVM normally loads them as a side effect of AWT/Cocoa bootstrap — that does not happen in a native image.
- Any class that uses Objective-C runtime classes (NSWindow, NSApplication, CAMetalLayer, etc.) MUST explicitly load the required frameworks in its static initializer:
  ```java
  SymbolLookup.libraryLookup("/System/Library/Frameworks/AppKit.framework/AppKit", Arena.global());
  SymbolLookup.libraryLookup("/System/Library/Frameworks/QuartzCore.framework/QuartzCore", Arena.global());
  ```
- Do this BEFORE any `objc_getClass(...)` calls, otherwise all class lookups silently return `nil` and every Objective-C message send is a silent no-op returning `0`.

### 11.5 Native Library Path
- LWJGL `.dylib` natives MUST be placed physically next to the native binary (in `Contents/MacOS/` for `.app` bundles).
- Set `org.lwjgl.librarypath` to the directory containing the dylibs at startup. In a GraalVM native binary, use `ProcessHandle.current().info().command()` to locate the executable path dynamically — `user.dir` is unreliable (resolves to `/` when launched via Finder).
- `build_native.sh` handles this automatically for the native build. Do not remove this logic.

### 11.6 What Is Safe to Use
- ✅ `java.lang.foreign.*` — fully supported
- ✅ `java.lang.invoke.MethodHandle` — fully supported
- ✅ `Arena.global()`, `Arena.ofConfined()`, `Arena.ofShared()` — fully supported
- ✅ Off-heap raw `long` pointers and `MemorySegment` — fully supported
- ✅ `ProcessHandle` — fully supported
- ✅ Static initializers deferred via `--initialize-at-run-time` — fully supported
- ❌ `java.awt.*`, `javax.swing.*` — NOT supported in native image
- ❌ Dynamic class loading (`Class.forName(...)` on unknown classes) — NOT supported without reflection config
- ❌ `static final MethodHandle` fields in runtime-initialized classes — constant-folded to `null`
- ❌ Assuming any system framework is pre-loaded — always load explicitly

